{
  "version": "v1.73.3",
  "strategy": "immutable-upstream-binaries",
  "platforms": [
    "darwin-arm64",
    "darwin-amd64",
    "linux-amd64",
    "windows-amd64"
  ],
  "notes": "Place upstream rclone binaries in resources/rclone/<os-arch>/rclone(.exe). Fero resolves a developer override path first, then the bundled binary. End-user releases must not rely on a system-installed rclone."
}
